# IDD - CI Polling (Lite)

Follow the full `idd-ci.instructions.md` file. Poll project checks only,
fail closed on missing or failing CI, and return to E1 after CI settles. The
`no-advisory` profile has no advisory-convergence exception.
